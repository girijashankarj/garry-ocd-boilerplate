#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

MSG_FILE=$1
MSG=$(cat "$MSG_FILE")

# Validate commit message format: '<ticket-number>: <message>'
if ! printf "%s\n" "$MSG" | grep -qE '^[A-Za-z0-9-]+: .+'; then
  echo "Invalid commit message format. Expected '<ticket-number>: <message>'"
  exit 1
fi
