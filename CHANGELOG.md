# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added

- Sequelize (SQLite) example for `garry-backend-template` with `db:sync` and `db:seed` scripts and example `User` model.
- Full backend API handler template structure (handlers, schema JSON, logic stages, and API common utils).
- OpenAPI generation script from handler schemas.

### Changed

- CLI: interactive-only prompts (project name, type, path, confirmation).
- PR workflow: streamlined to core lint/format/test/changeset checks.
